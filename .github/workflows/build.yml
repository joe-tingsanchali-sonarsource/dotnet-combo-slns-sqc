name: SonarQube Cloud - Build and scan separate or both projects
on:
  push:
    branches:
      - main
      - feature/*
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch: null

jobs:
  build:
    name: Build and analyze
    runs-on: windows-latest
    steps:
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'zulu' # Alternative distribution options are available.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Cache SonarQube Cloud packages
        uses: actions/cache@v4
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache SonarQube Cloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner
      - name: Install SonarQube Cloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path ${{ runner.temp }}\scanner -ItemType Directory
          dotnet tool update dotnet-sonarscanner --tool-path ${{ runner.temp }}\scanner
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v47
        with:
          files: |
            .github/**
            dotnet-angular/**
            dotnet-xunit-coverlet/**
      - name: Run step if any file(s) specified above have changed
        if: steps.changed-files.outputs.any_changed == 'true'
        shell: bash
        env:
          ALL_CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}
        run: |
          echo "One or more files has changed."
          echo "List all the files that have changed: $ALL_CHANGED_FILES\n"
          for file in ${ALL_CHANGED_FILES}; do
            echo "$file was changed"
          done
      # Determine which projects to scan based on changed files.
      # A .github change triggers all scans (workflow change may affect any project's analysis).
      - name: Determine scans to run
        id: scans
        if: steps.changed-files.outcome == 'success' && steps.changed-files.outputs.any_changed == 'true'
        shell: bash
        run: |
          github_changed="${{ contains(steps.changed-files.outputs.all_changed_files, '.github') }}"
          angular_changed="${{ contains(steps.changed-files.outputs.all_changed_files, 'dotnet-angular') }}"
          xunit_changed="${{ contains(steps.changed-files.outputs.all_changed_files, 'dotnet-xunit-coverlet') }}"
          echo "angular=$([[ $github_changed == 'true' || $angular_changed == 'true' ]] && echo true || echo false)" >> $GITHUB_OUTPUT
          echo "xunit=$([[ $github_changed == 'true' || $xunit_changed == 'true' ]] && echo true || echo false)" >> $GITHUB_OUTPUT
      - name: Build and analyze dotnet-angular
        if: !cancelled() && steps.scans.outputs.angular == 'true'
        shell: powershell
        run: |
          ${{ runner.temp }}\scanner\dotnet-sonarscanner begin /k:"joe-tingsanchali-sonarsource-1_dotnet-combo-slns-sqc-angular" /o:"joe-tingsanchali-sonarsource-1" /d:sonar.token="${{ secrets.SONAR_TOKEN }}" /d:sonar.links.scm="https://github.com/joe-tingsanchali-sonarsource/dotnet-combo-slns-sqc" /d:sonar.qualitygate.wait=true
          dotnet build dotnet-angular/Dotnet-Angular.sln
          ${{ runner.temp }}\scanner\dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
      - name: Build and analyze dotnet-xunit-coverlet
        if: !cancelled() && steps.scans.outputs.xunit == 'true'
        shell: powershell
        run: |
          ${{ runner.temp }}\scanner\dotnet-sonarscanner begin /k:"joe-tingsanchali-sonarsource-1_dotnet-combo-slns-sqc-dotnet" /o:"joe-tingsanchali-sonarsource-1" /d:sonar.token="${{ secrets.SONAR_TOKEN }}" /d:sonar.links.scm="https://github.com/joe-tingsanchali-sonarsource/dotnet-combo-slns-sqc" /d:sonar.qualitygate.wait=true
          dotnet build dotnet-xunit-coverlet/XUnit.Coverage.sln
          ${{ runner.temp }}\scanner\dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
